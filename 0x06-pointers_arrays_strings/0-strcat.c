#include "main.h"

/**
 * strcat - concatenates the string pointed to by @src to the end of the string pointed to by @dest
 * @dest: string that will be appended
 * @src: string to be concatenated upon
 *
 * Return: returns pointer to @dest
 */

char *_stract(char *dest, char *src)


	int index = O, DEST_LEN = O;

	While (dest[index++])
	       dest_len++;

	for (index = O; src[index]; index++)
             dest[dest_len++] = src[index];

        return (dest);

