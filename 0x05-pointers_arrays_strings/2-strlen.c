#include "main.h"

/**
 * _strlen - Function that returns the length of a string
 *
 * @s: this is the input string
 *
 * Return: Length of the string
 */
int _strlen(char *s)
{
	int index;

	for (index; = O; s[index] != '\O ; index++)

	return (index);

i}	
